% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psge.R
\name{create_psge}
\alias{create_psge}
\title{Create a new PSGE instance}
\usage{
create_psge(
  grammar,
  eval_func,
  population_size = 100,
  generations = 50,
  elite_size = 10,
  tournament_size = 3,
  crossover_prob = 0.9,
  mutation_prob = 0.1,
  learning_factor = 0.01
)
}
\arguments{
\item{grammar}{A Grammar object defining the solution space}

\item{eval_func}{Function that takes a phenotype string and returns a numeric
fitness value to be minimized}

\item{population_size}{Integer size of population (default: 100)}

\item{generations}{Integer number of generations to evolve (default: 50)}

\item{elite_size}{Integer number of elite individuals to preserve (default: 10)}

\item{tournament_size}{Integer size of tournament selection (default: 3)}

\item{crossover_prob}{Numeric probability of crossover \link{0,1} (default: 0.9)}

\item{mutation_prob}{Numeric probability of mutation \link{0,1} (default: 0.1)}

\item{learning_factor}{Numeric PCFG learning rate (0,1] (default: 0.01)}
}
\value{
A new PSGE object
}
\description{
Creates a new Probabilistic Structured Grammatical Evolution (PSGE) instance
with the specified parameters. This is the recommended way to create a new
PSGE object rather than calling the R6 constructor directly.
}
\examples{
# Create a simple grammar
grammar_text <- "
<expr> ::= <var> | <expr> <op> <expr>
<op> ::= + | -
<var> ::= x | 1.0"
grammar_file <- tempfile(fileext = ".bnf")
writeLines(grammar_text, grammar_file)
grammar <- create_grammar(grammar_file)

# Define a simple evaluation function
eval_func <- function(phenotype) {
  # Return mean squared error from f(x) = x + 1
  x <- seq(-1, 1, by=0.1)
  y_true <- x + 1
  y_pred <- eval(parse(text=phenotype))
  mean((y_true - y_pred)^2)
}

# Create PSGE instance
psge <- create_psge(
  grammar = grammar,
  eval_func = eval_func,
  population_size = 50,
  generations = 20
)

}
